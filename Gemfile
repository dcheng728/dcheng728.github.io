source "https://rubygems.org"

# Use the GitHub Pages gem to match the production build environment on Pages.
gem "github-pages", group: :jekyll_plugins

# Jekyll (and some dependencies) still expect webrick on Ruby 3+.
gem "webrick", "~> 1.8"
